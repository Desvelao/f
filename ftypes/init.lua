
-- @table ftypes
return {
    table = require('ftypes.table'), -- table: functional helpers to table type
    string = require('ftypes.string'), -- table: functional helpers to string type
    p = require('ftypes.table').print -- func: table print function
}